{
  "schema": "l9.quality-threshold-selection/v1",
  "profiles": {
    "pr_fast": {
      "sdk_policy": "semgrep-finding-policy.yaml"
    },
    "merge": {
      "sdk_policy": "semgrep-finding-policy.yaml"
    },
    "nightly": {
      "sdk_policy": "semgrep-finding-policy.yaml"
    },
    "release": {
      "sdk_policy": "semgrep-finding-policy.yaml"
    },
    "supply_chain": {
      "sdk_policy": "semgrep-finding-policy.yaml"
    }
  },
  "note": "Core selects an SDK policy file but never evaluates finding thresholds itself. sdk_policy is the bare semgrep-finding-policy.yaml filename, which the pinned resolver resolves under .github/governance before the SDK reads it; strict profiles reject a run whose findings that policy does not classify. The companion semgrep-identity-map.yaml is passed by .github/workflows/l9-analysis.yml."
}

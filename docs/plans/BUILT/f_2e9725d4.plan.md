---
name: Fix stale l9-ci-core pin
overview: Bump the pinned Quantum-L9/l9-ci-core commit SHA in .github/workflows/l9-analysis.yml (on PR
todos:
  - id: bump-sha-2
    content: Replace all 9 occurrences of d2c2cd7f7251632825c53b8ed31570ba5d8add26 with 89dcb36e6cbf5a14a777a3ce38134ce20269a251 in .github/workflows/l9-analysis.yml on branch fix/unified-remediation-phases-1-7 (PR
    status: completed
  - id: verify-local-2
    content: grep-verify zero remaining d2c2cd7f... occurrences in the file
    status: completed
  - id: commit-push-2
    content: Commit and push to fix/unified-remediation-phases-1-7 (PR
    status: completed
  - id: confirm-ci-2
    content: Re-check gh pr checks 2 to confirm Publish analysis (Core) / publish now passes alongside Analyze (semgrep -> SDK)
    status: completed
isProject: false
---

# Second bump: fix `Publish analysis (Core) / publish`

## Status of the first fix (done, confirmed passing)

The original problem (`Analyze (semgrep -> SDK)` failing with `ModuleNotFoundError: No module named 'yaml'`) is fixed and verified. Applied on `fix/unified-remediation-phases-1-7` (PR #2's branch, not the originally-assumed `ci/l9-v2-setup`/PR #3, which had already merged by execution time) — commit `026cbeb`, pushed. `gh pr checks 2` confirms `Analyze (semgrep -> SDK)`: **pass**.

## New problem, found while verifying the first fix

`Publish analysis (Core) / publish` now fails with:

```
provision-sdk: sdk-revision is not listed in .l9/sdk-compatibility.yaml
```

Root cause, same shape as the first bug, one layer deeper: `.github/workflows/l9-analysis.yml`'s `publish` job calls Core's reusable workflow `publish-analysis.yml@d2c2cd7f7251632825c53b8ed31570ba5d8add26` (our current pin). At that specific Core commit, `publish-analysis.yml` internally calls its own nested `provision-sdk` step pinned to an older, un-synced Core SHA (`4b6b7336576490185a313e5d71be9ad9755f3e98`), and passes it `sdk-revision: 6368ba17a98231d461a13b71e149e114ad766834` (the outer job's current default). That nested `4b6b7336...` checkout's own `.l9/sdk-compatibility.yaml` predates the SDK-pin bump, so it rejects `6368ba17...` as an unlisted revision.

Confirmed via `Quantum-L9/l9-ci-core` (https://github.com/Quantum-L9/l9-ci-core) that this is already fixed upstream:

- Commit `f88116503430` — **"fix(publish-analysis): bump internal action refs to d2c2cd7 so publish provisions the current SDK pin (#33)"** (2026-07-20T07:07:19Z) — bumps `publish-analysis.yml`'s internal `provision-sdk`/`validate-bundle`/`render-publication`/`publish-check` refs to `d2c2cd7f7251...`, resolving the exact mismatch above.
- Current Core `main` HEAD is `89dcb36e6cbf5a14a777a3ce38134ce20269a251` (re-verified live). Diffed `f88116503430...89dcb36e6cbf5a14a777a3ce38134ce20269a251`: only docs/template files and Core's own internal `governance-ci.yml` changed — no functional action code (`provision-sdk`, `invoke-sdk`, `validate-bundle`, `route-artifacts`, `build-artifact-manifest`, `publish-analysis.yml`, `resolve-governance`) differs between the minimal fix commit and current HEAD. Safe to pin to HEAD rather than the minimal fix commit.

## Fix

In [.github/workflows/l9-analysis.yml](.github/workflows/l9-analysis.yml) on branch `fix/unified-remediation-phases-1-7` (PR #2), replace every occurrence of `d2c2cd7f7251632825c53b8ed31570ba5d8add26` with `89dcb36e6cbf5a14a777a3ce38134ce20269a251` (9 occurrences: `L9_CORE_REF` plus the 8 `uses:`/reusable-workflow refs — `resolve-governance`, `provision-sdk`, `invoke-sdk` x2, `validate-bundle`, `route-artifacts`, `build-artifact-manifest`, `publish-analysis.yml`).

Same surgical, value-only bump as the first fix — no other lines in the file change.

## Verification

1. Apply the 9 replacements on `fix/unified-remediation-phases-1-7`.
2. `grep -c d2c2cd7f7251632825c53b8ed31570ba5d8add26 .github/workflows/l9-analysis.yml` locally to confirm zero remaining occurrences.
3. Commit, push, and re-check `gh pr checks 2` — expect `Publish analysis (Core) / publish` to flip to pass, with `Analyze (semgrep -> SDK)` remaining green.

## Not in scope

- Closing the 4 partial Dependabot PRs (#4-#7) proposing a subset of the original bump — queued separately in `memory-bank/tasks.md`, not part of this fix.
- The `SonarCloud Code Analysis` check failing on PR #2 — unrelated to `l9-ci-core`, pre-existing, out of scope.
- No further changes to `.github/governance/*.yaml` or any `.l9/` manifest in this repo.

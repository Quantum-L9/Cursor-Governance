#!/usr/bin/env bash
# preflight.sh — CG-PE-REMEDIATION-PR-006-v2.7.0
# Auto-generated by claude-coding-contract-compiler v2.7.0
# Run before starting this Claude Code session. Exits 1 if any precondition is unmet.
set -euo pipefail

echo "[preflight] CG-PE-REMEDIATION-PR-006-v2.7.0"
echo "[preflight] Checking prerequisites..."

FAILED=0

printf '[preflight] Verifying: %s\n' 'test "$(git rev-parse --abbrev-ref HEAD)" = claude/program-execution-authority-remediation-v1'
if ! test "$(git rev-parse --abbrev-ref HEAD)" = claude/program-execution-authority-remediation-v1 > /dev/null 2>&1; then
  printf '[preflight] FAIL: %s\n' 'test "$(git rev-parse --abbrev-ref HEAD)" = claude/program-execution-authority-remediation-v1'
  FAILED=$((FAILED+1))
fi

printf '[preflight] Verifying: %s\n' 'test "$(git show -s --format=%s HEAD)" = CG-PE-REMEDIATION-PR-005-v2.7.0'
if ! test "$(git show -s --format=%s HEAD)" = CG-PE-REMEDIATION-PR-005-v2.7.0 > /dev/null 2>&1; then
  printf '[preflight] FAIL: %s\n' 'test "$(git show -s --format=%s HEAD)" = CG-PE-REMEDIATION-PR-005-v2.7.0'
  FAILED=$((FAILED+1))
fi

printf '[preflight] Verifying: %s\n' 'git merge-base --is-ancestor 5eff2cdb27d709d37a9ee79fe8c2bc42515ff19d HEAD'
if ! git merge-base --is-ancestor 5eff2cdb27d709d37a9ee79fe8c2bc42515ff19d HEAD > /dev/null 2>&1; then
  printf '[preflight] FAIL: %s\n' 'git merge-base --is-ancestor 5eff2cdb27d709d37a9ee79fe8c2bc42515ff19d HEAD'
  FAILED=$((FAILED+1))
fi

printf '[preflight] Verifying: %s\n' 'python3 -m pytest -q environment/program-execution/scripts/tests/test_campaign_tunnel_airtight.py environment/program-execution/campaigns/scripts/test_close_campaign.py'
if ! python3 -m pytest -q environment/program-execution/scripts/tests/test_campaign_tunnel_airtight.py environment/program-execution/campaigns/scripts/test_close_campaign.py > /dev/null 2>&1; then
  printf '[preflight] FAIL: %s\n' 'python3 -m pytest -q environment/program-execution/scripts/tests/test_campaign_tunnel_airtight.py environment/program-execution/campaigns/scripts/test_close_campaign.py'
  FAILED=$((FAILED+1))
fi

printf '[preflight] Verifying: %s\n' 'python3 -m pytest -q environment/program-execution/peer_execution/tests/test_failover_policy.py environment/program-execution/scripts/tests/test_run_campaign.py -k "failover or provider or peer"'
if ! python3 -m pytest -q environment/program-execution/peer_execution/tests/test_failover_policy.py environment/program-execution/scripts/tests/test_run_campaign.py -k "failover or provider or peer" > /dev/null 2>&1; then
  printf '[preflight] FAIL: %s\n' 'python3 -m pytest -q environment/program-execution/peer_execution/tests/test_failover_policy.py environment/program-execution/scripts/tests/test_run_campaign.py -k "failover or provider or peer"'
  FAILED=$((FAILED+1))
fi

if [ "$FAILED" -ne 0 ]; then
  echo "[preflight] FAILED: $FAILED precondition(s) unmet."
  echo "[preflight] HALT RESUME_PRECONDITION_NOT_SATISFIED"
  exit 1
fi

echo "[preflight] All preconditions met. Safe to begin."

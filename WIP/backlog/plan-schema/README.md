# Plan schema backlog

`canonical.schema.plan_document.v1.yaml` — richer YAML meta-schema
(`execution_envelope`, DAG, property evidence matrix, baseline lock).

Live SSOT today: `skills/l9-plan/schemas/plan-document.schema.json` (thinner
JSON Schema). Promote only if intentionally evolving `l9-plan` beyond the
current contract.

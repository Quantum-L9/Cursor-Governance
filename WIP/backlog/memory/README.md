# Memory backlog

`graphiti-memory-integration-waves/` — wave/handoff notes for
`l9-graphiti-memory` ↔ Cursor-Governance generated-data integration.

CG producers largely live under `environment/agents/generated-data/`.
Session hydrate/close SSOT is `ops/graphiti/hydration/` (not these notes).
Keep only while cross-repo wave work remains open; delete when closed.

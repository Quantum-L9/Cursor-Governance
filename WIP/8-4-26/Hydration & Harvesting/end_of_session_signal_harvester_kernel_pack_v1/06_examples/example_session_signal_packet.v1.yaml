packet_id: session_signal_packet_example_001
session_id: example_session
trigger: context_window_near_limit
canonical_decisions:
- Task contract runtime emits CompiledTaskLoadPacket, not generic task instructions.
laws_to_add:
- No full context by default.
- No full kernel stack by default.
signals:
- signal_id: signal_001
  signal_type: task_contract_signal
  summary: Runtime must compile the slice to load and the contract to obey into one packet.
  source_evidence:
    session_refs:
    - user correction on task contract output
    evidence_level: explicit_user_instruction
  reuse_value:
    score: 92
    reason: Changes future coding-agent preflight behavior and reduces context load.
  promotion_decision: promote
  target_loop:
  - task_contract_runtime
  - coding_agent_preflight
  - context_budgeting
  confidence: confirmed
  unknowns: []
artifacts_current:
- l9_task_contract_runtime_schema_pack_v1.zip
artifacts_superseded:
- l9_task_operating_contract_runtime_spec_v1.yaml
constellation_updates:
- Task contract runtime remains a contract issuer, not a kernel orchestrator.
next_best_actions:
- Generate task runtime implementation contract from schema pack.
do_not_promote:
- Full transcript
validation_status: not_run

# Only needed if you use a PAT-based third-party Linear MCP bridge.
# The official server uses OAuth and needs NO secret. Prefer that.
#
# Copy to .env.local (git-ignored). Never commit a real key.
# Generate at: Linear > Settings > Account > Security & access > Personal API keys

LINEAR_ACCESS_TOKEN=lin_api_replace_me

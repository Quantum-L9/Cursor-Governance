# Operator front-door. Fill this. Then compile.
# Do not copy this file into environment/program-execution/campaigns/<id>/.
#
# python3 skills/l9-pe-campaign-activate/scripts/compile_activation_files.py \
#   --intent "WIP/8-15-26/Campaign Activation Files/TEMPLATE/INTENT.yaml" \
#   --repo-root "$(pwd)"

campaign_id: your-campaign-id
title: Your Campaign Title
owner: Igor Beylin

objective: >-
  One paragraph. What this campaign must make true.

problem_statement: >-
  What is wrong or missing today. Defaults to objective if omitted.

target_state: >-
  What is true when the campaign is done. Defaults to objective if omitted.

target:
  repository_id: Quantum-L9/Cursor-Governance
  source_of_truth: path/the/work/touches
  adapter: git

tasks:
  - title: Lock current state
    objective: Record the live baseline before any edit.
    paths: []
  - title: Implement the change
    objective: Edit only the declared paths. Local commit. Do not push.
    paths:
      - path/to/file

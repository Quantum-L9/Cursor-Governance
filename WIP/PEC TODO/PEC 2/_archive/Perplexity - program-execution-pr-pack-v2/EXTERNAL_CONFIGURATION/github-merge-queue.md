# External configuration: GitHub merge queue
Enable "Require merge queue" for main. Merge method: squash. Confirm
required checks trigger on both pull_request and merge_group. Set batch
size per ops/config/merge-queue.config.yaml.

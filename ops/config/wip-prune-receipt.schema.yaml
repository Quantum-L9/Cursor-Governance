# Schema: l9.wip.prune_receipt.v1
# Written by ops/scripts/wip_corpus.py prune|hygiene under WIP/_receipts/.
type: object
required: [schema, created_at, removed]
properties:
  schema:
    const: l9.wip.prune_receipt.v1
  created_at:
    type: string
  removed:
    type: array
    items:
      type: object
      required: [wip_path, landed_path, sha256, action]
      properties:
        wip_path: {type: string}
        landed_path: {type: string}
        sha256: {type: string}
        action:
          const: removed

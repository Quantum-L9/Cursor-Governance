# Schema: l9.wip.inventory.v1
# Generated by ops/scripts/wip_corpus.py inventory|hygiene. Committed SSOT index.
type: object
required: [schema, updated, entries]
properties:
  schema:
    const: l9.wip.inventory.v1
  updated:
    type: string
    description: UTC timestamp YYYY-MM-DDTHH:MM:SSZ
  entries:
    type: array
    items:
      type: object
      required: [path, kind, sha256, status]
      properties:
        path:
          type: string
        kind:
          enum: [dated, series, receipt]
        date_bucket:
          type: ["string", "null"]
        sha256:
          type: string
        status:
          enum: [active, possible-landed]
        note:
          type: ["string", "null"]
        landed:
          type: ["object", "null"]
          properties:
            path: {type: string}
            pr: {type: ["string", "integer", "null"]}
            sha: {type: ["string", "null"]}

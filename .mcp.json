{
  "_generated": "Rendered from environment/agents/adapters/claude-code/mcp.template.json by ops/scripts/claude_projection.py (domain: mcp). Do not edit \u2014 edit the template. Consumer-owned servers not defined by the template are preserved.",
  "mcpServers": {
    "l9-graphite-memory": {
      "type": "stdio",
      "command": "${HOME}/.cursor-governance/ops/memory/run_memory_mcp.sh",
      "args": [
        "-m",
        "l9_graphite_memory.server",
        "--transport",
        "stdio"
      ]
    },
    "context7": {
      "type": "http",
      "url": "https://mcp.context7.com/mcp",
      "timeout": 120000,
      "headers": {
        "Authorization": "Bearer ${CONTEXT7_API_KEY}"
      }
    }
  }
}

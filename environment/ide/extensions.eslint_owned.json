{
  "schema": 1,
  "class": "eslint_owned",
  "comment": "Additional extensions for workspaces where ESLint (not Biome) owns lint/format — e.g. Website-Bot, SEO-Bot. Biome is still installed from the core set but is never set as defaultFormatter for these workspaces.",
  "extensions": [
    "dbaeumer.vscode-eslint",
    "esbenp.prettier-vscode"
  ]
}

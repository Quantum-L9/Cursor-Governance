{
  "_comment": [
    "AUTHORITATIVE memory MCP for Claude Code \u2014 Cursor Graphiti front door only.",
    "Same store/lifecycle as ops/graphiti/graphiti_memory_client.py (CANONICAL_LAW \u00a78).",
    "Requires the Graphiti SSH tunnel (sessionStart ensure_graphiti_tunnel.sh) and",
    "GRAPHITI_MCP_TOKEN from ~/.cursor/graphiti.env / Keychain / secrets.",
    "FORBIDDEN: retired HTTP l9-shared-memory control-plane side door and L9_MEMORY_HTTP_* lifecycle env.",
    "Adapters must not reintroduce a second memory plane."
  ],
  "mcpServers": {
    "graphiti-memory": {
      "url": "http://127.0.0.1:8100/mcp/",
      "headers": {
        "Authorization": "Bearer ${GRAPHITI_MCP_TOKEN}"
      },
      "timeout": 120000,
      "alwaysLoad": true
    }
  }
}

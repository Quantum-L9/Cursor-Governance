{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": [
    "Committable .claude/settings.json for an L9 consumer repo. Copy to .claude/settings.json",
    "and commit it: on Claude Code Web/Mobile only git-tracked files survive the clone, so a",
    "committed settings.json is what makes governance boot with no manual step. On the CLI the",
    "same file is picked up locally. Keep secrets OUT of this file — they belong in the account",
    "environment (web/environment.env.example) or user-scope config, never in a repo.",
    "Field/event names track the Claude Code release; see https://code.claude.com/docs.",
    "permissions.allow lists read-only GitHub queries, PR monitoring (subscribe/unsubscribe),",
    "and PR create/update so requested work opens a PR without a prompt. Only the stable",
    "mcp__github__* server is listed; merge/destructive tools (merge_pull_request, force pushes)",
    "are intentionally omitted so they still prompt."
  ],
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PROJECT_DIR/.claude/hooks/session_start_claude_governance.sh\"",
            "timeout": 30
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Read",
      "Grep",
      "Glob",
      "Bash(git status:*)",
      "Bash(git diff:*)",
      "Bash(git log:*)",
      "Bash(make claude-env:*)",
      "mcp__github__pull_request_read",
      "mcp__github__get_check_run",
      "mcp__github__actions_get",
      "mcp__github__actions_list",
      "mcp__github__get_job_logs",
      "mcp__github__get_commit",
      "mcp__github__list_commits",
      "mcp__github__list_pull_requests",
      "mcp__github__search_pull_requests",
      "mcp__github__get_file_contents",
      "mcp__github__list_branches",
      "mcp__github__list_tags",
      "mcp__github__list_releases",
      "mcp__github__get_latest_release",
      "mcp__github__get_me",
      "mcp__github__issue_read",
      "mcp__github__list_issues",
      "mcp__github__search_issues",
      "mcp__github__search_code",
      "mcp__github__subscribe_pr_activity",
      "mcp__github__unsubscribe_pr_activity",
      "mcp__github__create_pull_request",
      "mcp__github__update_pull_request"
    ],
    "deny": [
      "Read(./.env)",
      "Read(./.mcp.json)",
      "Bash(rm -rf:*)"
    ]
  },
  "env": {
    "L9_GOVERNANCE_SURFACE": "claude-code"
  }
}

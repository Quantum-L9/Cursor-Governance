{
  "_comment": "Graphiti HTTPS front door at ${GRAPHITI_MCP_URL}. NO bearer. The capability-broker experiment never shipped \u2014 do not point this server at L9_CAPABILITY_BROKER_URL. Peer of environment/agents/adapters/claude-code/mcp.template.json.",
  "mcpServers": {
    "graphiti-memory": {
      "url": "${GRAPHITI_MCP_URL}",
      "timeout": 120000,
      "alwaysLoad": true
    }
  }
}

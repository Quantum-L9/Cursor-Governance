{
  "_comment": "Graphiti HTTPS front door (${GRAPHITI_MCP_URL}). NO bearer. The capability broker never shipped and is retired. Peer of environment/agents/adapters/claude-code/mcp.template.json.",
  "mcpServers": {
    "graphiti-memory": {
      "url": "${GRAPHITI_MCP_URL}",
      "timeout": 120000,
      "alwaysLoad": true
    }
  }
}

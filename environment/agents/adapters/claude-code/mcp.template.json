{
  "_comment": [
    "Claude Code MCP configuration — Graphiti HTTPS front door.",
    "Authority: environment/agents/adapters/claude-code/mcp.template.json is the",
    "SINGLE canonical owner. The project-scope .mcp.json is RENDERED from this file",
    "by ops/scripts/claude_projection.py (domain: mcp) and drift-checked — it is a",
    "projection, never a second authority.",
    "",
    "NO CREDENTIALS in this file or in the account environment. Graphiti is",
    "${GRAPHITI_MCP_URL} (default https://memory.quantumaipartners.com/graphiti/mcp).",
    "The capability broker (L9_CAPABILITY_BROKER_URL / broker.quantumaipartners.com)",
    "never shipped and is retired; do not point MCP at it.",
    "",
    "FORBIDDEN: GRAPHITI_MCP_TOKEN in server config, the retired HTTP l9-shared-memory",
    "side door, L9_MEMORY_HTTP_* lifecycle env, and ${L9_CAPABILITY_BROKER_URL}/mcp/graphiti."
  ],
  "mcpServers": {
    "graphiti-memory": {
      "type": "http",
      "url": "${GRAPHITI_MCP_URL}",
      "timeout": 120000,
      "alwaysLoad": true
    }
  }
}

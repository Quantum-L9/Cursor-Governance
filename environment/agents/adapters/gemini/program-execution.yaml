# L9_META
#   l9_schema: 1
#   repo: Quantum-L9/Cursor-Governance
#   path: environment/agents/adapters/gemini/program-execution.yaml
#   layer: adapter-binding
#   owner: governance-control-plane
#   status: active
#   version: 1.0.0
#   updated: 2026-08-10
#
# Peer-execution cross-link (plan section 6). Gemini's role is `reviewer`, so
# its program adapter is a `verifier` (independent verification), never a
# worker host. Cross-validated by `make peer-execution-conformance`.
schema: l9.agent-program-execution-binding.v1
agent_id: gemini
surface: gemini
binding_kind: agent
program_execution:
  enabled: true
  adapters:
    - gemini-review

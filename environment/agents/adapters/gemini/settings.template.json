{
  "_l9_meta": {
    "l9_schema": 1,
    "repo": "Quantum-L9/Cursor-Governance",
    "path": "environment/agents/adapters/gemini/settings.template.json",
    "layer": "adapter-config",
    "owner": "governance-control-plane",
    "status": "active",
    "version": "2.0.0",
    "updated": "2026-09-06",
    "note": "Merge mcpServers into ~/.gemini/settings.json. Canonical memory control plane only: l9-graphite-memory (MemoryService over stdio) rendered when ${L9_MEMORY_INTERPRETER} names a Python carrying the pinned package (ops/config/memory-binding.json). No env block, no credential value. The legacy direct-provider front door (graphiti-memory over a provider URL) was retired at realignment stage C7/C8; the capability broker never shipped. Peer of environment/agents/adapters/claude-code/mcp.template.json."
  },
  "mcpServers": {
    "l9-graphite-memory": {
      "command": "${L9_MEMORY_INTERPRETER}",
      "args": ["-m", "l9_graphite_memory.server", "--transport", "stdio"],
      "trust": false,
      "_requires_env": ["L9_MEMORY_INTERPRETER"]
    }
  }
}

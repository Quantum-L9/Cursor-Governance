# claude-code-bounded-autonomy

Kind: `worker_host`. Provider: `claude_bounded_autonomy`.

This adapter consumes an exact Controller-rendered contract, narrows authority, emits digest-bound lifecycle receipts, and never declares program convergence.

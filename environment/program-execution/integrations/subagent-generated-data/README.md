# Generated-Data Outcome Publisher

Execution outcomes are compiled into the existing generated-data packet format
and submitted through the canonical processor. This integration never writes
directly to Graphiti and never creates another outbox or state database.

# Remote Actions

Remote action support is an optional adapter, not part of the universal Controller core. An adapter must validate exact action approval, Blueprint ceiling, Program Lock digest, task and target identity, base and candidate revisions, prerequisite evidence, expiry, and forbids. It must emit a durable action receipt. Connector availability is never approval.

# Adapter Conformance

Conformance is fail-closed. Positive fixtures must pass. Negative and hostile
fixtures must fail for the declared reason. Conformance never proves a live host
capability; that requires a fresh read-only capability probe.

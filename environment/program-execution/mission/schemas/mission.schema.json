{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "program-execution-system/mission.v1",
  "title": "Mission Revision",
  "description": "An immutable, digest-bound Mission Revision: durable parent intent above one or more Execution Programs. Mission declares outcomes and ceilings; it never prescribes Task Cards, Runtime Tasks, waves, worktrees, files to edit, worker prompts, leases, provider bindings, execution profiles, or runtime task state. Authority: ADR-0024, ADR-0025.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema",
    "mission_id",
    "mission_revision",
    "mission_owner",
    "objective",
    "acceptance_criteria",
    "authority_ceiling"
  ],
  "properties": {
    "schema": {
      "description": "Schema discriminator. Exactly one supported value in v1.",
      "const": "l9.program-execution.mission.v1"
    },
    "mission_id": {
      "description": "Immutable Mission identity. Stable across every revision of the same Mission.",
      "type": "string",
      "pattern": "^MISSION-[A-Z0-9-]+$"
    },
    "mission_revision": {
      "description": "Revision ordinal. Immutable per revision; authoritative change creates a superseding revision rather than rewriting this one.",
      "type": "integer",
      "minimum": 1
    },
    "mission_owner": {
      "description": "Canonical owner of the Mission definition, the Mission Revision, and the Mission verdict. Required, non-empty after normalization, authoritative, and part of Mission digest identity. Changing it requires a superseding Mission Revision.",
      "type": "string",
      "minLength": 1
    },
    "objective": {
      "description": "Durable outcome statement. Intent, never a plan.",
      "type": "string",
      "minLength": 1
    },
    "acceptance_criteria": {
      "description": "Mission Acceptance Criteria. Mission-owned; Program acceptance may contribute evidence toward these but never implies them (ADR-0027).",
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["criterion_id", "statement"],
        "properties": {
          "criterion_id": {
            "type": "string",
            "pattern": "^MAC-[A-Z0-9-]+$"
          },
          "statement": {
            "type": "string",
            "minLength": 1
          }
        }
      }
    },
    "authority_ceiling": {
      "description": "Outer authorization ceiling over the Mission, expressed in the existing Program Execution action vocabulary. Lower layers may narrow it and never widen it. A ceiling, not an instruction. Detailed ceiling/budget/termination semantics are governed by MISSION_AUTHORITY_SCOPE_BUDGET_CONTRACT and are not implemented by this contract.",
      "$ref": "program-execution-system/action-authorization.v2"
    },
    "targets": {
      "description": "Declarative Mission targets. Scope, budget, and termination semantics belong to MISSION_AUTHORITY_SCOPE_BUDGET_CONTRACT and are not implemented by this contract.",
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "metadata": {
      "description": "Non-authoritative annotation. Incapable of changing authorization, scope, acceptance, budgets, termination, or ownership. Structurally forbidden planning/runtime fields must not be smuggled in here.",
      "type": "object",
      "additionalProperties": true,
      "not": {
        "anyOf": [
          {"required": ["tasks"]},
          {"required": ["task_cards"]},
          {"required": ["waves"]},
          {"required": ["worktrees"]},
          {"required": ["files_to_edit"]},
          {"required": ["worker_prompts"]},
          {"required": ["leases"]},
          {"required": ["provider"]},
          {"required": ["provider_ref"]},
          {"required": ["execution_profile"]},
          {"required": ["runtime_task_state"]}
        ]
      }
    }
  }
}

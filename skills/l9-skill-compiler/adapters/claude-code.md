<!-- L9_META
schema: 1
parent: l9-skill-compiler
layer: adapter
role: platform-adapter
version: 1.0.0
status: active
-->

# Claude Code Adapter

Use the canonical skill directory without rewriting its doctrine. Install only to a path verified from the active Claude Code environment. Add `allowed-tools` only when the user requests a restricted tool surface and the platform accepts that field. Preserve relative references and run the bundled validators before installation.

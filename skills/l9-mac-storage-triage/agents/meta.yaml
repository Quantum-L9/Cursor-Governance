# Adapter display metadata for this Skill pack.
# Canonical path: agents/meta.yaml (never agents/openai.yaml).
interface:
  display_name: "Mac Storage Triage"
  short_description: "Diagnose-first macOS disk reclaim: caches, old Docker, trash"
  brand_color: "#1D4ED8"
